import Login from '../../components/login'

export default function Home() {
  return (
    <main className="flex w-screen h-screen items-center justify-center bg-white text-black">
      <Login/>
    </main>
  );
}
